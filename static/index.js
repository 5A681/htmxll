// function initializeDatePicker() {
//     const datepickerInput = document.getElementById("datepicker-format");
//     if (datepickerInput) {
//         // Replace this with your date picker library's initialization
//         datepickerInput.datepicker = new Datepicker(datepickerInput, {
//             format: "dd-mm-yyyy", // Customize as needed
//         });
//     }
// }

// // Initialize on page load
// document.addEventListener("DOMContentLoaded", initializeDatePicker);

// // Reinitialize after HTMX updates
// // document.body.addEventListener("htmx:afterSettle", initializeDatePicker);